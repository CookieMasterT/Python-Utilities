Scripts intended to be used by modules so that certain actions are centralized

default_config.json exists to avoid hardcoding all default values

(for example reading the config file is done from 1 place)